import { makeStyles } from '@material-ui/core';

const useStyles = makeStyles(theme => ({
  table: {
    backgroundColor: '#196725',
    width: '100%',
    borderRadius: 20,
  },
  stats: {
    backgroundColor: '#d08d8e',
    width: '100%',
    borderRadius: 20,
  },
  cardImage: {
    width: 100,
  }
}));

export default useStyles;
